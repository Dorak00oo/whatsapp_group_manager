/**
 * Altura total de la barra de navegación móvil (safe-area + fila de tabs).
 * Debe coincidir con `dashboard-mobile-chrome` y el backdrop del menú «Más».
 */
export const MOBILE_TOP_NAV_TOP_CSS =
  "calc(3.625rem + max(0.5rem, env(safe-area-inset-top, 0px)))";
